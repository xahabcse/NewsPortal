{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Read(**)",
      "Edit(**)",
      "Write(**)",
      "Glob(**)",
      "Grep(**)",
      "mcp__playwright__*",
      "mcp__browser__*",
      "PowerShell(if \\($env:GH_TOKEN\\) { \"GH_TOKEN set\" } elseif \\($env:GITHUB_TOKEN\\) { \"GITHUB_TOKEN set\" } else { \"No GH token env\" })"
    ],
    "defaultMode": "bypassPermissions"
  }
}
